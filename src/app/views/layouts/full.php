<?php if(isset($page) && isset($content)): ?>
    <section id="blz-app">
        <main>
            <div>
                <?php echo $content; ?>
            </div>
        </main>
    </section>
<?php endif; ?>